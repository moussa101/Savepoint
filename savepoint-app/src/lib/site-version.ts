/** Site release metadata — bump when shipping a new announced version. */
export const SITE_VERSION = 'v1.0.0';

/** Accounts created at/after this instant are "new" and skip the confetti modal. */
export const UPDATE_RELEASED_AT = new Date('2026-09-07T21:00:00.000Z');

export const RELEASE_FORUM_SLUG = 'savepoint-updates';
export const RELEASE_TOPIC_TITLE = `${SITE_VERSION} — Messaging, PSN, forums & safety`;

export const SITE_UPDATE_ID = `sp-release-${SITE_VERSION}`;
